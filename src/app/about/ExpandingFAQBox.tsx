import { useState } from "react"

const ExpandingFAQBox = ({ text, type }: { 
  text: string,
  type: string
}) => {
  const [expanded, setExpanded] = useState<boolean>(false);

  return (
    <div>

    </div>  
  )
}